{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Sample Contract",
  "type": "object",
  "required": ["name", "age"],
  "properties": {
    "name": {
      "type": "string",
      "description": "User's full name"
    },
    "age": {
      "type": "string",
      "description": "User's age as string (breaking change with proper major bump)"
    },
    "email": {
      "type": "string",
      "format": "email"
    }
  }
}
