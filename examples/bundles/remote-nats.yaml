nats:
  url: "${NATS_URL:-nats://127.0.0.1:4223}"
stream:
  name: "${RITUAL_STREAM_NAME:-RITUAL_EVENTS}"
  subjects: ["demon.ritual.v1.>"]
  duplicateWindowSeconds: 120
operateUi:
  baseUrl: "${OPERATE_UI_URL:-http://127.0.0.1:3000}"
seed:
  enabled: true
  runs:
    - runId: "bundle-run-b"
      ritualId: "echo-ritual"
      gates:
        - gateId: "approval-1"
          requester: "ops@example.com"
          ttlSeconds: 5
