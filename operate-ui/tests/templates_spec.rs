#[test]
#[ignore]
fn run_detail_renders_approval_rows() {
    // Given fixtures approval.requested/granted/denied in contracts/fixtures/approvals
    // When render run_detail.html with those events in history
    // Then HTML contains "Pending approval", "Granted", "Denied" badges without 500s
    unreachable!("agent will implement");
}
